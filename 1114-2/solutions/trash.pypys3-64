V, T = map(int, input().split())
length = 109
distance = V * T
position = (distance) % length
final_position = (position + 1) % length
if final_position == 0:
    final_position = length
print(final_position)